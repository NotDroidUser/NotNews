package com.notdroid.notnews.db.newsApi

/* *
 * This represents the param searchIn in the api, and the posible values that it can have
 * */
enum class SearchInValues(){
  title,
  description,
  content
}